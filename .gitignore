# Dependencies
node_modules/

# Build scratch (the published output in /site IS committed by the pipeline)
wiki-export/
*.log
.DS_Store
Thumbs.db
npm-debug.log*

# F&O change tracker: keep fo-baseline.json (the documented snapshot); the diff report is ephemeral
build/fo-changes.json
